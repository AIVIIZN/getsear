{
  "name": "sear-print-relay",
  "version": "1.0.0",
  "description": "Sear POS local print relay — receives print jobs via HTTP and sends to network thermal printers via TCP",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npx tsx index.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
