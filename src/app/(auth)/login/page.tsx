export default function LoginPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-[var(--foreground)]">Login</h2>
      <p className="mt-2 text-sm text-[var(--muted-foreground)]">
        Sign in to your Sear POS account.
      </p>
    </div>
  );
}
