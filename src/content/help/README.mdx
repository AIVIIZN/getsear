# Sear Help Source

The V8.1 help drawer renders the versioned help registry from `src/app/(backoffice)/help/content.ts`.
This MDX content directory is reserved for article-by-article authoring once the content pipeline moves
from typed registry entries to compiled MDX.
